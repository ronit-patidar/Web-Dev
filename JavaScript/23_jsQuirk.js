console.log(typeof null);  // object

// 🤔 Why is typeof null "object"?
// It's a historical bug (mistake) in JavaScript from 1995.

// In the early version, values were represented as type tags internally.

// null got the same type tag as an object by mistake.

// And because JavaScript cannot break old websites (backward compatibility), they never fixed it.

// 🤔 Why is typeof null "object"?
// It's a historical bug (mistake) in JavaScript from 1995.

// In the early version, values were represented as type tags internally.

// null got the same type tag as an object by mistake.

// And because JavaScript cannot break old websites (backward compatibility), they never fixed it.